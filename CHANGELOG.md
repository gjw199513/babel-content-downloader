# 0.1.26 — 五语言宽屏界面与使用文档

- 扩展介绍跟随浏览器语言，品牌名保持 Babel Content Downloader。
- 点击扩展图标打开宽屏概览页；扩展选项打开独立设置页。
- 概览页与设置页支持跟随浏览器、英语、简体中文、繁体中文、日语、韩语；浏览器无匹配语言时回退英文，手动选择可记忆并可恢复自动模式。
- 概览页只保留连接状态、三步用法和常用请求；接入命令、暂停／恢复／撤销与技术信息移至设置页或折叠详情。
- 介绍、连接状态、接入与复制说明、使用示例和常见错误提示同步切换；原始技术诊断保留在折叠详情中。
- README 提供五语言完整安装、使用、排错和维护指南。命令、API 和下载原内容保持不变。

# 0.1.25 — 首个交付版本

- 保留统一网页提取、浏览器回退、音视频与字幕保存。
- 导出元数据记录已验证的音轨提取、合轨、转码和裁切行为。
- 避免把推荐文章的日期当作当前正文发布日期。
- 首版安装及已知限制见 [安装说明](docs/first-release.md)；15 个登记来源不代表所有页面均已验证。

# 版本记录

## 0.1.24

- 普通文章的已有URL标签与显式tab目标使用统一DOM提取；tab读取绑定事先观察的页面地址，旧扩展不接收新协议。
- auto/bundle检测到页面内嵌音视频或嵌入对象时转交平台处理；未取得资源保留partial，不把仅图文报告成完整收集。明确document仍保存图文。
- metadata新增已校验交付文件摘要，记录相对路径、大小、SHA及已有媒体验证字段；不推断未记录的转换动作。
- 43文件/338测试、typecheck与12文件LSP通过；真实浏览器与发行包验收另记。

## 0.1.23 — 通用浏览器正文回退

- 普通网页HTTP无法读取时，对已许可的博客路由调用同一个通用DOM提取器；不增加逐站正文选择器。
- 当前页浏览器回执独立于HTTP回执，无需站点终点或双快照；加载中、截断和缺图保留部分完成。登录/付费/私密门控保持明确失败。
- 新增OpenAI官方文章导航路由与精确站点权限，受控测试通过，真实浏览器验收待扩展重载。
- 新策略仅发送给0.1.23及以上扩展；既有平台规则版本保持2026.09.20.7，不改变其他平台采集策略。

## 0.1.22 — 普通网页统一采集

- 普通博客／SSR 文章统一使用 HTTP + Mozilla Readability，保留技术正文与图片，遗漏结构化内容时回退通用主内容区域。
- 文档 URL 任务不依赖扩展连接；加入编码、压缩、体积、重定向与当前页完整性检查，并接入原任务导出／续跑。
- 只升级运行时；扩展保持 0.1.21，无新增扩展权限或重载要求。公开 HTTP 被拒绝的页面仍报告真实限制。

## 0.1.21 — Hugging Face 数据集说明卡完整性规则

- 为公开、非 gated 的 dataset 说明卡增加独立规则，要求精确 DatasetHeader 身份、唯一正文根、无加载占位和两次至少 700ms 的稳定采样。
- 规则仅证明说明卡正文，不下载数据行、文件树或权重；model／paper 保持独立规则，blog 不继承。
- 来源对比确认 all-nli 说明卡的代码、引用和末尾保留；旧 .20 partial 任务不改写。新版本实际浏览器回验待加载扩展后执行。

## 0.1.20 — Hugging Face 模型卡完整性规则

- 为公开、非 gated 的 model 卡片增加独立正文完整性规则：精确身份、唯一正文根、无加载占位、两次至少 700ms 稳定采样。
- dataset／blog 不继承此规则；不新增模型权重或数据集下载。
- 规则版本更新为 2026.09.20.6；扩展需加载 0.1.20 后单独进行真实回验。

## 0.1.19 — 平台字幕头部兼容与精确语言选择

- 支持实测 WebVTT 的有界 Kind／Language 头字段，保留 cue、时间轴和尾部完整性检查。
- 指定字幕语言时优先精确匹配；通用语言没有精确来源时保留原变体匹配，具体变体仍必须精确匹配。交付保留实际语言和来源，不在精确轨校验失败后静默换轨。
- 仅升级运行时，扩展保持 0.1.18，字节未变。

## 0.1.18 — 资料包入口与内容脚本错误分类

- 仅已识别的 Chrome 无接收端错误进入有界等待；端口关闭与未知拒绝立即返回脱敏的 CONTENT_SCRIPT_TRANSPORT_FAILED，避免把所有传输异常当作页面尚未就绪并重复发送。
- 保留挂起消息的 20 秒响应预算、无效响应检查和同任务安全复用；不扩展平台权限或新增浏览器操作。
- 纯媒体／附件的 bundle 增加 content.md 本地导航，独立于来源正文；部分完成后续跑会更新入口，有正文时安全替换旧入口，不虚增 text 完成状态。
- 增加音频成功后立即清理已登记临时文件且保留用户文件的回归。
- 本版修改扩展消息处理；源码和测试结果不等于实际浏览器故障已修复，加载后另行验证。

## 0.1.17 — 慢加载页面的有界自动等待

- 仅正文根暂不可用的 retryable ADAPTER_CHANGED 改为间隔 60 秒、240 秒的两次自动重试；单次桥接仍为 20 秒，其他临时错误沿用原重试规则。
- 等待沿用持久 retry_at，释放并发与来源名额，支持取消；正常运行下复用同任务自有静音标签页。
- 仅升级运行时；扩展保持 0.1.16，规则保持 2026.09.20.5。源码、构建与真实回归结果分别记录。

## 0.1.16 — 有界结构诊断与访问判定读取优化

- 现有 observe 增加仅 Medium 文章／YouTube watch 的静态结构诊断，无新增工具或选择器参数；公开唯一根才可返回限定 input／figure 祖先结构，最多 48 节点、每链 6 层，截断明示。
- 不返回正文、HTML、URL或表单值；桥接严格验证字段、父链、访问状态和请求内容身份，MCP 仅转发白名单字段。
- 单次访问判定复用根节点文本和可见性，保留访问优先级与短路行为；每次观察重新读取。
- 本批用于补充结构证据与减少重复读取，不宣称已经解决 Medium 正文污染或 YouTube／X 采集失败。

## 0.1.15 — 页面就绪重试与正文图片

- 页面正文根暂未就绪时纳入现有最多两次自动重试，继续沿用同任务自有静音页与 20 秒单次命令上限；访问门控和适配器执行异常不放宽。
- Medium 从正文 figure 内的 picture/source 读取已声明图片来源，排除作者头像；picture 必须有唯一 img，保留原图说明与正文顺序。订阅区域清理和真实回验尚未完成。
- 运行时和扩展目标版本 0.1.15，规则 `2026.09.20.4`；构建与浏览器结果分开记录，不据源码修复宣称平台已通过。

## 0.1.14 — 一手资料完整性规则

- GitHub README、Hugging Face paper 详情、Anthropic engineering、DeepMind publication 与 arXiv abs 增加限定路由的稳定采样规则；同站其他路由不继承完整性。
- arXiv 正文外的目标 PDF 使用独立附件证明，继续绑定论文版本并校验文件；不会伪造正文资源顺序。
- DeepMind publication 文档用途自动要求全文 PDF，普通博客和明确的其他用途保持原选择。
- 本批修改扩展规则，运行时与扩展目标版本均为 0.1.14，规则 `2026.09.20.3`；源码验证与实际浏览器结果分别记录。

## 0.1.13 — 运行时徽章 SVG 修复

- 增加有界静态 SVG 徽章支持，保留原字节，继续校验内部引用并拒绝外部资源与活动内容；由隔离 worker 完整渲染验证。
- 仅升级运行时；扩展继续使用 0.1.12，规则为 `2026.09.20.2`。正式／开发扩展构建字节与前版完全一致，无需重新加载。
- 31 文件／241 项测试、TypeScript、89 文件 LSP 零诊断及构建通过；106 个输入未变。实际 MCP 已确认运行时 0.1.13 与已连接的扩展 0.1.12。浏览器与本地包结果见对应验收记录。


## 0.1.12 — 真实页面缺口修复

- GitHub 支持当前标题组件的 Public 标记；可见性沿祖先链核对，避免隐藏旧模板参与公开性判断。
- arXiv 增加桌面可见 PDF 入口；同论文／版本限制保留，重复的移动与桌面控件只生成一个文件。
- 支持经过严格解析和隔离像素校验的静态路径型 SVG，原字节保存；含外部资源、脚本或尚未支持结构的 SVG 仍不接受。
- 修复依据来自 0.1.11 真实页面和已下载头图；新版浏览器回归尚待完成。

## 0.1.11 — AI／学习资料来源范围

- 按修订 Spec 固定 15 个来源目标，保留原 9 站并实现 GitHub、Hugging Face、arXiv、Anthropic、Google DeepMind 的实验性单内容规则。OpenAI 官网结构核对受 403 验证阻挡，保持未实现。
- 删除 16 个旧基线适配和全部 13 个候选，以及相应页面权限、注入与快手专用等待逻辑；旧任务和用户资料作为历史保留。
- 技术正文新增代码、表格、公式结构，保留缩进、合并单元格、来源 TeX 和可用引用链接；正文和表格执行一致的凭据脱敏。
- arXiv 保存文档时必须包含目标论文全文 PDF，显式版本严格匹配；缺少文件时返回部分完成。
- 构建前清理已生成的运行时代码，防止已删除适配器残留在分发包中。

本批统一测试、构建和浏览器验收状态见实施记录；新增规则的 HTTP 结构和受控测试不作为真实浏览器通过证据。

以下 0.1.10 及更早记录为历史，包含已退出范围的平台，不再形成当前计划。

## 0.1.10 — 历史本地构建，原三站计划已撤销

- 本地候选修订 2 明确区分源码构建与预编译 TGZ 安装，补齐固定目录、编译 CLI 和扩展路径说明；发行物旁提供完成的本地清单和校验文件，包内 bootstrap manifest 继续明确标为模板。
- 适配器声明的排除子树不再受 wrapper 可见性过滤；隐藏控件、评论及其资产仍保持排除，避免搜狐隐藏“放大看”文字重新进入正文。
- 跳过按钮界面文字，同时保留已登记媒体在段落／引用中的原始顺序；不从按钮额外发现来源，未登记媒体继续阻止完整性升级。
- 快手仅对自有页面、同一精确作品、公开免费且已匹配播放器但缺少视频来源的快照，在现有 20 秒期限内等待来源。新门控或适配错误立即保留原结果；来源仍未就绪时保留 partial 文字并标记 MEDIA_SOURCE_NOT_READY。仅加入实测精确 CDN 域名，不扩大域名通配规则。

28 个文件／230 项测试、TypeScript、83 文件 LSP 零诊断和正式／开发构建通过，95 个构建与测试输入未变。运行时 initialize 已确认 0.1.10；9 月 20 日实际扩展也已确认 0.1.10。随后用户变更来源范围：搜狐任务终态 blocked，快手／X 未提交，旧计划撤销。此前平台缺口保留为历史，未公开发布。

## 0.1.9 — 已完成五站样本回归，仍有缺项

- 抖音直接视频页以页面中的精确作品 ID 绑定目标，分别读取正文信息区和播放器区，排除评论及推荐；支持标准 video/audio `currentSrc`，继续使用原有 HTTP(S) 与来源域名校验。展开只作用于正文，静音播放只作用于已绑定的播放器。
- X 按原文保存帖子正文，不再将整段帖子当作文章标题；作者限于目标作者区域，按实际 DOM 结构清理时间、互动及图片说明控件。保留原文中的同名文字，缺少证据的分隔符和首次采集缺图原因不宣称已修复。
- 欢迎页接入说明改为 macOS 托管运行时安装与健康检查，真实客户端重载后还须调用连接检查；其他系统如实说明尚无托管启动支持。

规则版本 `2026.09.19.5`。27 个文件／221 项测试、TypeScript、82 文件 LSP 零诊断及正式／开发构建全部通过，94 个构建与测试输入在检查期间未变。主运行时 initialize 与实际扩展心跳均已确认 0.1.9，五站真实回归已完成：搜狐文件保存通过但控件清理未生效，快手无视频，Facebook 登录门控，抖音超时，X 正文与可见原文一致但缺图。补证后的修复属于后续源码，不算本版已通过。既有版本包和历史浏览器证据保持原样。

## 0.1.8 — 本地构建，等待真实扩展回归

- 搜狐按实际图片行结构排除“放大看”控件子树，保留图片顺序和作者可能使用的同名文字。
- 快手新增实测的单视频详情根、播放器与作者选择器，完整排除评论文字、头像和表情图片。
- Facebook 新增已观察的 pfbid 直接帖子地址；仅在唯一同页面／同帖子自链接和唯一明确公开标记均成立后，允许忽略已确认的顶部登录横幅。只提取唯一目标 message，操作也限定在该正文内；配图核心区域尚未证实，返回 `MEDIA_CORE_NOT_EVIDENCED` 并保持 unknown。数字 ID 原地址不假定与 pfbid 等同；全 FB 提取保守排除已观察的完整评论子树。
- macOS 新增本项目运行时的后台安装、启动、状态、停止和卸载。所有权核对精确路径和文件指纹，健康检查验证实际 MCP 版本；升级或配置变化后重启同一服务，不接管其他进程，不删除采集文件。
- 增加 Agent 安装说明与未发布的 bootstrap manifest 模板。Windows／Linux 托管启动、真实 Agent 客户端加载仍未验收。

本版统一验证通过：26 个文件／216 项测试、TypeScript、81 文件 LSP 零诊断、正式／开发构建；93 个构建与测试输入在检查期间未变。规则版本为 `2026.09.19.4`。113 文件源码归档独立安装、49 个编译文件一致；真实 launchctl 隔离验证通过父命令退出后持续服务、两个 stdio 客户端、启停与卸载。实际扩展回归尚未完成；0.1.7 浏览器结果保持原记录。Instagram 正文／评论边界、Threads 路由漂移以及其他平台已有缺口不因本批源码修改而改为通过。

## 0.1.7 — 本地构建，已完成四站样本回归

- 扩展心跳上报当前版本，运行时仅在已鉴权且会话匹配后刷新显示；兼容没有版本字段的旧扩展。无效版本不会改变状态或取走待处理命令，重载后沿用旧会话也能更新版本。
- 搜狐图片来源补齐真实正文 `data-src` 使用的精确 CDN 域名，排除已确认的 `sohu-default.png` 占位来源；不把占位图保存成功当成正文配图完整。
- 头条公开文章按实际嵌套 ARTICLE 提取正文，在同一唯一文章容器内识别作者和时间；新增限定 `/article/:id` 的按次完整性规则，仍须双快照稳定、资源完整且顺序一致。微头条及视频路由未获得此规则。
- 支持已观察的 Facebook 公开帖子描述 slug 加数字 ID 地址，并保留原地址；拒绝不完整帖子路径和主页／信息流伪装。
- 页面命令、闲置清理、关闭事件的记录更新和撤销清理按序执行，避免旧记录覆盖正在使用或新建页面的状态；关闭事件处理失败会保留待重试。静音和用户页面保护规则不变。

统一检查通过：25 个测试文件／202 项测试（`--maxWorkers=2`）、TypeScript、79 个 TS 文件的 LSP 零诊断、正式及开发扩展构建；91 个构建／测试输入的哈希在检查期间未变。实际运行时与扩展均为 0.1.7。四站新任务中，搜狐正文及 14 图共 17 文件、头条正文及 2 图共 5 文件完整交付并通过独立核验；搜狐仍混入 14 处“放大看”控件文字。Facebook 返回 LOGIN_REQUIRED，Instagram 返回 ADAPTER_CHANGED，均未交付文件，规则问题继续修复。

本批未改 Pinterest 选择器：0.1.6 同页后续观察已能识别现有登录 gate，早先采集失败按原结果保留。上述四个样本不代表全部平台或全部用途通过。

## 0.1.6 — 本地构建，已完成五站样本回归

- 调整内容脚本为 DOM 就绪后的 `document_end` 注入，页面准备与内容响应共用从命令开始计算的 20 秒期限。持续投递失败与等待响应超时分别返回错误，适配器错误保持原样；后者仍受最多两次自动重试约束。
- 根据真实 Substack、搜狐、头条和企鹅号页面证据修正正文、元数据与界面区域的边界；完整性规则仍需本次采集的实际边界与稳定快照证明。
- 正文中可见但未被适配规则识别的图片、视频、音频或嵌入媒体会阻止生成完整性证明，避免静默漏项后误报完整。受控反例已复现并修复，不代表已发现真实平台丢失内容。
- Substack 保留文章头的独立副标题；元数据容器不唯一时不生成完成证明。Pinterest 依据实测增加两种登录遮罩识别。

统一检查通过：24 个测试文件／193 项测试（`--maxWorkers=2`）、TypeScript、77 个 TS 文件的 LSP 零诊断、正式及开发扩展构建。首次默认并发有两项触及 5 秒测试期限；降低并发后全部通过，未改变断言或期限。运行时与新注册扩展均为 0.1.6：Substack、企鹅号各一个公开文章样本完整交付并通过独立产物校验；搜狐和头条仍 partial，Pinterest blocked，详见浏览器联调记录。

本轮发现旧会话缓存注册版本的问题：扩展重载后沿用 session，心跳活跃并不更新版本字段。此前看到 0.1.5 不能据此断定用户未重载。确认任务闲置后仅重启本项目运行时，扩展自动重新注册并报告 0.1.6。刷新版本的源码补丁和定向测试已补，尚未进入本版构建；历史版本标签按当次注册报告保留。

历史错误只支持内容脚本准备时序的诊断假设，没有记录实际注入时刻，新修复仍需真实回归。[Chrome 注入时机说明](https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts#run_time)用于选择 DOM 已就绪的注入阶段，不作为本站点修复成功的证据。

## 0.1.5 — 候选包

- 修复损坏的字幕、PDF 和伪 Office 附件仅凭文件头就被标记为完整的问题。字幕解析完整时间与正文；PDF 检查交叉引用并在独立进程中逐页解析；ZIP、Office 和 EPUB 检查压缩包、校验和及必需文档结构。
- 修复有合法尺寸头但像素数据已截断的图片仍被标记完整的问题；独立解析进程检查全部像素及动画帧，保持来源文件字节不变。GIF 额外核对完整块流，防止解码器只恢复首帧后报告成功。
- 续跑重新验证已保存的字幕与文档附件。旧无效文件保留在磁盘，但不再进入本次成功清单；有效缓存继续复用。
- PDF 校验不执行文档动作、不渲染或访问文档中的网络资源，解析有时间、内存和容量上限。无法确认时返回缺项及原因。
- 新增固定版本 PDF.js、XML 与图片解析依赖，最低 Node.js 版本改为 22.13.0；正常安装须保留 sharp 的系统二进制依赖。

完整源码测试 23 个文件／166 项通过，TypeScript 通过，LSP 76 个 TS 文件／0 errors／0 warnings。两份官方 PDF、四类官方文档样本与 50 张既有真实图片通过源码兼容性检查。正式／开发构建、独立生产安装、已安装解析器及 0.1.3 → 0.1.5 → 0.1.3 运行时升级回滚通过。实际运行时 0.1.5／扩展 0.1.4（扩展代码与 0.1.5 相同）已完成一个知乎公开专栏的正文及 24 图完整交付；Substack 六张图已恢复但正文仍 partial，新增搜狐／头条／网易／企鹅号尝试结果见浏览器联调记录。

## 0.1.4 — 候选包

- 浏览器检查按 `instance_ref` 分别报告在线、暂停和离线，并与任务路由使用相同的心跳期限；旧实例不会因另一个浏览器在线而显示可用。
- 完成按次、受信任路由的正文完整性 proof：规则绑定适配器、URL、内容 ID、内容类型和边界；静态资格、资源顺序／未放置资源和关联范围均须通过。带稳定性要求的规则以 WeakMap 保存内部资格，只在同页只读双快照一致后写入 SHA-256 stability proof。
- bridge、运行时和续跑采集均重新验证 proof 与当前 adapter／URL binding；持久化 snapshot 经过脱敏，恢复时不会仅凭旧的 `complete` 字符串放行。
- 根据真实知乎公开专栏 DOM 收窄正文为精确 RichText 根；作者与 `datePublished` 仅从同一文章容器读取，“编辑于”只作为文章末端边界而不作为发表时间。该专栏规则要求同一文章内相邻终点与两次至少 700 ms 的稳定采样；回答路由未启用该规则。
- 修复 Substack 含逗号代理 URL 的 `srcset` 解析，并将正文图片限定在已观察到的 article body figure 范围；`images.unsplash.com` 仅为该受控正文图片来源加入许可，未放宽任意外链图片。修复同一图片 URL 在两个正文 occurrence 被静默折叠的问题，同时避免播放器与内部 `<source>` 重复成媒体组。
- yt-dlp 的**最终**诊断为 `handshake timed out`（含 `handshake operation timed out`）时归类为既有可重试 `NETWORK_TIMEOUT`；早期警告不会覆盖最终 403、证书等错误，重试仍遵循最多两次的有界策略。
- 统一源码验证已通过：`npm test` 19 个文件／131 个测试，`npm run typecheck` 0 错误，LSP 65 个 TypeScript 文件／0 error／0 warning。

0.1.4 尚未完成新扩展包的真实浏览器 E2E，不升级任何平台 coverage，也不把 0.1.3 的历史媒体或浏览器证据追溯为本版本通过。构建、独立安装及 0.1.3 → 0.1.4 → 0.1.3 运行时升级回滚已通过；Chrome 重新加载及真实平台复验仍待完成，构建目录版本不作为已加载版本。

## 0.1.3 — 2026-09-19（联调修复）

- 图片没有尺寸属性时不再生成 `width/height: 0`；运行时兼容已安装旧扩展的零尺寸，非法响应立即返回明确字段诊断。
- 页面提取忽略 DOM 注释等非正文节点，修复框架占位注释造成的异常；扩展消息响应有真正的超时限制。
- 内容脚本暂未就绪时，在同一任务内最多自动重试两次，等待期间仍可取消；自动重试复用该任务已经加载的自有静音页。
- 保留多层行内包装节点中的正文段落和图片顺序，避免整篇正文压成一段、图片集中到末尾；排除的引用区域不会经包装节点重新混入。
- 文档不再重复输出与首个正文一级标题相同的标题，保留正文后续有意重复的标题。
- 已取得真实扩展连接、自有图文／播客文件保存、实际自有任务标签静音与导航复用证据；这些受控样本不计为第三方平台全部通过。
- 真实 YouTube／Bilibili 单视频保存音频已通过扩展 → MCP → 本地文件闭环，独立核验了产物哈希、完整静默解码与 seek；macOS 独立运行时安装和 0.1.2 → 0.1.3 → 0.1.2 升级回滚检查通过。
- Bilibili 单视频保存 MP4 在网络握手超时后续跑成功；1080p H.264 + AAC 文件的大小、哈希、完整静默解码与 seek 独立核验通过。

真实平台联调仍在进行。知乎新快照已恢复段落与图片交错顺序，目录混入与正文完整性仍待解决；音频成功不代表同平台其他用途已通过。首次错误与后续成功分别保留，详见浏览器验收记录。未对外发布。

## 0.1.2 — 2026-09-18（本地开发包）

- 修复混合内容只有音视频时自动选择为空的问题；按已确认偏好和内容主体选择保存结果。确需澄清时返回简短用途选项，并通过 `job_resume(save_as)` 继续原任务。
- 续跑按来源指纹校验已保存附件和媒体缓存。同 ID 的资源发生可观察变化时重新保存，保留旧文件；只更新签名参数时继续复用已校验媒体。
- 文档链接按实际保存文件标注音频／视频，保留来源说明；普通正文中的 Markdown 标记保持为原文。
- 增加真实 YouTube、Bilibili 1080p 视频的静默文件验证记录；完整解码、时长和音视频流检查通过，记录限于媒体引擎。

仍待真实扩展配对、网站静音操作和逐平台采集验收。既有 0.1.0／0.1.1 本地包保持不变。

## 0.1.1 — 2026-09-18（本地开发包）

- 长文章与大量附件的 MCP 查询返回分页文件引用，完整内容保存在本地。
- 429／503 等瞬时错误最多自动重试两次；等待时可取消，保留已保存成果，遵循有界的 Retry-After。
- 临时文件及详细诊断默认保留七天。只清理本任务登记且哈希未变的临时文件，保留用户新增、修改的文件与所有最终资料。
- 可指定媒体工具的绝对路径并实际探测版本；新增 Codex／Claude 命名 MCP 条目的安装、状态检查和受控撤销。
- 支持明确的音频／视频格式、视频高度、媒体片段、字幕语言与图片序号；不能满足时返回限制或缺项。
- 媒体完整性按用户请求的内容判断。单项媒体有有界来源、来源时长和完整解码证据时，无需把无关页面正文当作媒体缺项。
- 增加有界续帖／引用／转发关系数据及独立作者、附件保存。关系算法有受控夹具；真实 X／Threads 选择器仍未验证。
- 限定自动展开到目标正文；目标 permalink 使用精确内容 ID，排除前缀碰撞和嵌套引用。
- 保持加载网站前静音、后台处理不播放声音的默认行为。

完整浏览器闭环和逐平台真实覆盖仍未完成；此版本不是已验证全平台可用的正式发布。

## 0.1.0 — 2026-09-18（本地开发包）

独立 MV3 扩展、MCP 运行时、25 个主流平台的实验性适配规则、目的枚举、文档与媒体保存、检查点恢复、目录及网络边界、预先静音的任务标签页。独立运行时安装包通过本机启动和 stdio MCP 检查；真实 YouTube／Bilibili 音频仅完成媒体引擎验证。未对外发布。
